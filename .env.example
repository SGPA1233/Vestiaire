# Copiez ce fichier vers .env et complétez les valeurs avant de lancer
# l'application (docker compose up, ou npm run dev en local).

# --- Base de données ----------------------------------------------------------
# En local avec docker-compose, ces trois valeurs suffisent (le mot de passe
# est aussi utilisé pour construire DATABASE_URL automatiquement par compose) :
POSTGRES_USER=dotation
POSTGRES_PASSWORD=change-moi-en-un-mot-de-passe-long-et-aleatoire
POSTGRES_DB=dotation

# Utilisée directement par l'application en dehors de docker-compose
# (par ex. `npm run dev` avec une base PostgreSQL locale ou distante) :
DATABASE_URL="postgresql://dotation:change-moi@localhost:5432/dotation?schema=public"

# --- Authentification -----------------------------------------------------------
# Générez une valeur aléatoire, par ex. avec : openssl rand -base64 32
AUTH_SECRET="a-remplacer-par-une-chaine-aleatoire-longue"
NEXTAUTH_URL="https://dotation.entreprise.ch"

# --- Général ---------------------------------------------------------------------
APP_NAME="Dotation Vêtements"

# --- Envoi d'emails (réinitialisation de mot de passe) ---------------------------
# Facultatif : sans ces valeurs, le lien de réinitialisation est affiché à
# l'écran (un administrateur peut le transmettre manuellement). Avec un
# compte email professionnel hébergé chez Infomaniak, utilisez par exemple :
#   SMTP_HOST=mail.infomaniak.com
#   SMTP_PORT=587
#   SMTP_USER=contact@entreprise.ch
#   SMTP_PASSWORD=mot-de-passe-du-compte-email
#   SMTP_FROM=contact@entreprise.ch
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# --- Fonctionnalité facultative ---------------------------------------------------
# Signature tactile sur tablette après une perception (désactivée par défaut).
ENABLE_SIGNATURE=false

# --- Uniquement pour `npm run db:seed` (données de démonstration) ----------------
SEED_ADMIN_EMAIL=admin@entreprise.ch
SEED_ADMIN_PASSWORD=ChangeMoi123!
